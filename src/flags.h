#ifndef FLAGS_H
#define FLAGS_H

typedef struct nohex_flags {
    int help;
    int version;
    int nocolor;
    int offset;
    int ascii;
    int length;
    int cols;
    int skip;
} nohex_flags;

#endif
